import GameFilter from '../components/GameFilter';
import GameList from '../components/GameList';

const Home = () => {
  return (
    <main>
      <GameList />
    </main>
  );
};

export default Home;
