import { ReactElement } from 'react';
import GameList from '../components/GameList';

const Home = (): ReactElement => {
  return (
    <main>
      <GameList />
    </main>
  );
};

export default Home;
